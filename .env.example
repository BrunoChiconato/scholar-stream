# Preferred format: <orgname>-<account_name>  (e.g., myorg-demo)
SNOWFLAKE_ACCOUNT=your_org-your_account

# Your Snowflake username (check with: SELECT CURRENT_USER();)
SNOWFLAKE_USER=your_username

# Password for the user above (or leave empty if you use keypair/SSO outside this script)
SNOWFLAKE_PASSWORD=your_password

# Optional: default role/warehouse/database for the session.
# Note: the .sql files already run explicit USE ROLE/USE DATABASE statements where needed.
SNOWFLAKE_ROLE=SYSADMIN
SNOWFLAKE_WAREHOUSE=WH_TRANSFORM
SNOWFLAKE_DATABASE=SCHOLARSTREAM
